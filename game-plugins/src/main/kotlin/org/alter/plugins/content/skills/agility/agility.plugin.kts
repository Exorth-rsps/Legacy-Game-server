package org.alter.plugins.content.skills.agility

/**
 * Root agility plugin placeholder. As we flesh out individual courses their
 * scripts will simply register themselves with [AgilityCourseRegistry]. This
 * file intentionally stays lightweight so that we can hook future global
 * behaviour (e.g. shared dialogue, graceful set bonuses, etc.) in a single
 * location.
 */
// Intentionally left blank for now.
